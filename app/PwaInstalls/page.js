import PwaManager from './Components/PwaManager'

/**
 * PWA Installs Page - Monitor installations and send custom push notifications.
 * 
 * Standard structure following the admin app pattern.
 */
const page = () => {
    return <PwaManager />
}

export default page
